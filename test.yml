System:
  - Name: dockerd
    Build: dockerd
    Privileged: true
    Volumes:
      - /var/containers
Test:
  - Name: continuously
    Build: continuously
    Cmd: rake spec
    VolumesFrom:
      - dockerd
