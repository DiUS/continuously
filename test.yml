system:
  - name: dockerd
    build: dockerd
    privileged: true
    volumes:
      - /var/containers
test:
  - name: continuously
    build: continuously
    cmd: rake spec
    volumes-from:
      - dockerd
